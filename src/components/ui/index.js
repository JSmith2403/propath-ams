// Single import surface for the design-system primitives.
//
//   import { Button, Card, Input, Avatar } from '../ui';
//
// Phase 1 baseline. Phase 2 (Programme Builder) and Phase 3 (AMS ↔ App
// alignment) gradually replace inline styles with these primitives.

export { default as Button } from './Button';
export { default as Card }   from './Card';
export { default as Input }  from './Input';
export { default as Avatar } from './Avatar';
export { default as TabBar } from './TabBar';
